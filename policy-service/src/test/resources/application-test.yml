spring:
  datasource:
    url: jdbc:h2:mem:policydb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    driver-class-name: org.h2.Driver
    username: sa
    password: sa
  jpa:
    properties:
      hibernate.default_schema: public
app:
  jwt:
    secret: "test-secret-please-change-to-32+chars-xxxxxxxxxxxx"
    issuer: "policy-service-test"
    ttl-minutes: 30